(jbuild_version 1)

(library
 ((name        jbuilder)
;;  (public_name jbuilder)
  (libraries   (unix
		jbuilder_re
		jbuilder_opam_file_format
                usexp))
  (c_names     (xxhash digest))
  (synopsis    "Internal Jbuilder library, do not use!")))

(rule (copy ../vendor/xxHash/xxhash.c xxhash.c))
(rule (copy ../vendor/xxHash/xxhash.h xxhash.h))

(ocamllex (meta_lexer glob_lexer))
